<Project>

  <Target Name="SetAssemblyVersion">
    <PropertyGroup>
      <AssemblyVersion Condition="'$(AssemblyVersion)' == ''">1.0.0.0</AssemblyVersion>
    </PropertyGroup>
  </Target>

</Project>
